title: Jeffrey Rivor's blog
author:
  name: Jeffrey Rivor
description: >
  Principal Software Engineer at Microsoft since 2015.
  Opinions are my own.
show_excerpts: true

lang: en-US
timezone: America/Los_Angeles

# Jekyll [3.3, 4) requires user-configured excludes to manually prepend default excludes
exclude:
# defaults
- Gemfile
- Gemfile.lock
- node_modules
- vendor/bundle/
- vendor/cache/
- vendor/gems/
- vendor/ruby/
# additions
- LICENSE
- .github/
- .vs/
- .vscode/

remote_theme: jekyll/minima
minima:
  skin: auto
  social_links:
    - platform: github
      user_url: https://github.com/jeffreyrivor
    - platform: linkedin
      user_url: https://www.linkedin.com/in/jeffrey-r-9300913a

permalink: pretty
defaults:
  - scope:
      path: ""
      type: posts
    values:
      author: Jeffrey Rivor
      lang: en-US
  - scope:
      path: "*.html"
      type: pages
    values:
      layout: page
  - scope:
      path: "README.md"
    values:
      list_title: All Posts
header_pages:
  - posts.html
  - japan.html
feed:
  excerpt_only: true
readme_index:
  remove_orignals: true
titles_from_headings:
  strip_title: true
  collections: true

future: true # defaults from github-pages
jailed: false # defaults from github-pages
lsi: false # defaults from github-pages
highlighter: rouge # defaults from github-pages
markdown: kramdown # defaults from github-pages
commonmark: # config from github-pages for "GFM"
  extensions: ["table", "strikethrough", "autolink", "tagfilter"]
  options: ["unsafe", "footnotes"]
kramdown: # defaults from github-pages
  input: GFM
  hard_wrap: false
  gfm_quirks:
  - paragraph_end
  math_engine: mathjax
  syntax_highlighter: rouge
  syntax_highlighter_opts: 
    default_lang: plaintext
  template: ""
gist: # defaults from github-pages
  noscript: false
sass: # defaults from github-pages for production
  style: compressed
safe: true # defaults from github-pages
whitelist: # defaults from github-pages
  - jekyll-coffeescript
  - jekyll-commonmark-ghpages
  - jekyll-feed
  - jekyll-gist
  - jekyll-github-metadata
  - jekyll-paginate
  - jekyll-redirect-from
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-avatar
  - jemoji
  - jekyll-mentions
  - jekyll-relative-links
  - jekyll-optional-front-matter
  - jekyll-readme-index
  - jekyll-default-layout
  - jekyll-titles-from-headings
  - jekyll-include-cache
  - jekyll-octicons
  - jekyll-remote-theme
plugins:
# defaults from github-pages
  - jekyll-coffeescript
  - jekyll-commonmark-ghpages
  - jekyll-gist
  - jekyll-github-metadata
  - jekyll-paginate
  - jekyll-relative-links
  - jekyll-optional-front-matter
  - jekyll-readme-index
  - jekyll-default-layout
  - jekyll-titles-from-headings
# additions
  - jekyll-remote-theme
  - jekyll-feed
  - jekyll-octicons
  - jekyll-sitemap
